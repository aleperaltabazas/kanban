mutation CreateCard(
  $title: String!
  $description: String
  $priority: Int!
  $tasks: [TaskInput!]!
  $labels: [LabelInput!]!
  $status: StatusInput!
) {
  createCard(
    input: {
      title: $title
      priority: $priority
      description: $description
      tasks: $tasks
      labels: $labels
      status: $status
    }
  ) {
    card {
      ...RegularCard
    }
  }
}
