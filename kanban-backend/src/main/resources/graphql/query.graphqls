type Query {
    card(id: ID): Card
    cards: [Card!]!
}

type Card {
    id: ID!
    title: String!
    description: String
    priority: Int!
    tasks: [Task]
    status: Status
}

type Task {
    description: String!
    completed: Boolean!
    priority: Int
}

union Status = Backlog | WIP | Done

type Backlog {
    ref: String
}

type WIP {
    ref: String
}

type Done {
    ref: String
    completionDate: String
}
